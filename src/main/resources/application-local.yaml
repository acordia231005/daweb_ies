spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/instituto
    username: admin@local.com
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update   # create | update | validate | none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false